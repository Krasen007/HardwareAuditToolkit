<UserControl x:Class="HardwareAuditToolkit.App.Views.DashboardHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:HardwareAuditToolkit.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DashboardViewModel}"
             d:DesignHeight="540" d:DesignWidth="860">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid>
        <!-- Back / Export / Exit live in the persistent window header (MainWindow.xaml). -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="16">
            <ItemsControl ItemsSource="{Binding Modules}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:DashboardItemViewModel}">
                        <Border Width="240" Height="130" Margin="6" Padding="14"
                                CornerRadius="8" Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding OpenCommand}" />
                            </Border.InputBindings>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" />
                                    <Border Margin="8,0,0,0" Padding="6,1" CornerRadius="4"
                                            Background="#3F3F46" Visibility="{Binding IsExclusive, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="exclusive" FontSize="10" Foreground="#CCCCCC" />
                                    </Border>
                                </StackPanel>
                                <!-- Roadmap E3: the audit's shape at a glance — what ran,
                                     what passed, what's left — before exporting. -->
                                <TextBlock Text="{Binding StatusText}" FontSize="12" FontWeight="SemiBold"
                                           Foreground="#BBBBBB" Margin="0,6,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#BBBBBB" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StatusText}" Value="Passed">
                                                    <Setter Property="Foreground" Value="#9AE6B0" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusText}" Value="Failed">
                                                    <Setter Property="Foreground" Value="#FF8A80" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusText}" Value="Warning">
                                                    <Setter Property="Foreground" Value="#FFD479" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusText}" Value="Cancelled">
                                                    <Setter Property="Foreground" Value="#FFD479" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusText}" Value="Running">
                                                    <Setter Property="Foreground" Value="#9ECBFF" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#BBBBBB"
                                           TextWrapping="Wrap" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding Category}" FontSize="10" Foreground="#888888"
                                           Margin="0,6,0,0" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
