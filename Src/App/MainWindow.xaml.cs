using System.Windows;

namespace HardwareAuditToolkit.App;

/// <summary>
/// Main shell window. Phase 1 replaces this placeholder with the dashboard,
/// sidebar navigation, the persistent Exit overlay, and the Ctrl+E hook (§6).
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
